# Teaching data

Every record here is invented. The six-variant table is an arithmetic aid,
not a genomic scoring file with usable reference coordinates.

The larger recurring project is generated by `scripts/course_helpers.R`.
No real participant genotypes or outcomes are bundled. The fixed seed and
source code define the synthetic input; the end-to-end runner regenerates it.
